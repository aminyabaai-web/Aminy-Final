# Avatar — identity circle

Initials on a soft gradient, or a photo via `src`. Warm gradient for children, teal for adults.

```jsx
<Avatar name="Sarah" tone="teal" />
<Avatar name="Kai" tone="child" size={52} />
<Avatar src="/img/dr-morales.jpg" name="Dr. Morales" tone="care" />
```