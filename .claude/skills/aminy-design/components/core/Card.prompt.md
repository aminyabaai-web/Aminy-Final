# Card — elevated white surface

Floats on the mist background. Set `interactive` for tappable cards (lift on hover).

```jsx
<Card>…</Card>
<Card interactive onClick={open} padding={16}>…</Card>
```

White is elevated. Don't put a white card on a white page — let the mist do the lifting.