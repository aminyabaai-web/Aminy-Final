# Input — calm text field

16px font floor (prevents iOS zoom), 3px teal focus ring, sentence-case labels.

```jsx
<Input label="Note a moment" placeholder="What happened, in your words…" />
<Input label="Email" type="email" hint="We'll only use this to send your weekly report." />
```

Error copy names what happened and offers the next move — never blames the user.